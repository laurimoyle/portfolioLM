const clean=v=>String(v||'').trim();export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const{name,email,message,website}=req.body||{};if(website)return res.status(200).json({ok:true});const n=clean(name).slice(0,100),e=clean(email).slice(0,254),m=clean(message).slice(0,4000);if(!n||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e)||!m)return res.status(400).json({error:'Invalid form'});if(!process.env.FORMSPREE_FORM_ID)return res.status(503).json({error:'Form service not configured'});const r=await fetch(`https://formspree.io/f/${process.env.FORMSPREE_FORM_ID}`,{method:'POST',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({name:n,email:e,message:m,_subject:`Portfolio enquiry from ${n}`})});return r.ok?res.status(200).json({ok:true}):res.status(r.status===429?429:502).json({error:'Delivery failed'})}
